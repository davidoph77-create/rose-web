import { BrainV2Context, BrainV2Priority, BrainV2PriorityScore } from "./BrainTypes";
export class BrainV2PriorityEngine { readonly id="brain-v2-priority-engine"; readonly version="2.0.0"; score(c:BrainV2Context):BrainV2PriorityScore{let s=40; const r:string[]=[]; if(c.urgency==="immediate"){s+=45;r.push("La demande est indiquée comme urgente.")} else if(c.urgency==="high"){s+=30;r.push("La demande comporte une échéance proche.")} else if(c.urgency==="low"){s-=15;r.push("La demande peut attendre.")} if(c.needsDecision){s+=10;r.push("Une décision doit être préparée.")} if(c.needsPlanning){s+=8;r.push("Un plan d’action est nécessaire.")} if(c.domains.includes("finance")||c.domains.includes("property")){s+=8;r.push("Le sujet peut avoir un impact financier important.")} s=Math.max(0,Math.min(100,s)); return {score:s,priority:this.p(s),reasons:r.length?r:["Aucun facteur d’urgence particulier détecté."]};} private p(s:number):BrainV2Priority{return s>=85?"critical":s>=65?"high":s>=40?"medium":"low";} }
